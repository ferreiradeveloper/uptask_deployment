extends layout

include mixins/crearProyecto


block contenido
    .contenido-principal
        h1 #{nombrePagina}

        //pre= vardump(proyecto)

        if errores
            each error in errores
                .alertaErr= error.texto
        +crearProyecto(proyecto)
        
